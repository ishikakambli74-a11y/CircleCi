version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.11
    steps:
      - checkout
      - run:
          name: Run Python Test
          command: python Test.py

workflows:
  build_and_test:
    jobs:
      - build
